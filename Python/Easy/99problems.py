a = int(input())
total = format.round(a)
print(total - 1) # Rounds the input number and prints it
# The code reads an integer input, rounds it to the nearest whole number, subtracts one, and prints the result.

# Importatnt do not work